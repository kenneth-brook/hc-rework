window.HC911_CONFIG = Object.freeze({
    // Mapbox public browser token. Public tokens are intended to be visible
    // client-side; restrict this token to hamiltontn911.gov in Mapbox.
    mapboxAccessToken: ""
});
